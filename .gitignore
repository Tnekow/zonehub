# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build outputs and temp artifacts
dist/
dist-ssr/
build/
# Electron-builder (shares output dir `dist/` with Vite)
dist/win-unpacked/
build_output/
*.blockmap
# Electron TypeScript emit (see electron/tsconfig.json outDir)
electron/dist-electron/
/tmp
.trae/*
lint-report.json

# Legacy Steam redistributables (not used in local-only mode)
steam/

# Local env files
*.local
.env
.env.*
!.env.example

# Database schemas / dumps (never commit)
*.sql

# Dependencies
node_modules/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Package manager files
pnpm-lock.yaml
yarn.lock

# Tooling
.vercel
.cursor/
.cursorignore

# Steam local debug AppID
steam_appid.txt
**/steam_appid.txt