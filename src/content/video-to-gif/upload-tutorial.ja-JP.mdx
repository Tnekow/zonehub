import { CopyCodeBlock } from '../../components/mdx/CopyCodeBlock';

export const stepTitle = 'ステップ 4: Steam ワークショップアップロードチュートリアル';

const anonymousCode = `v_trim=_=>{return _},$J('#title').val(' \\n'+Array.from(Array(126),_=>'\\t').join(''));`;
const artworkCode = `$J('#ConsumerAppID').val(480),$J('[name=file_type]').val(0),$J('[name=visibility]').val(0);`;

<div className="space-y-6 not-prose">
  <div className="p-4 bg-blue-900/20 border border-blue-500/30 rounded">
    <p className="text-blue-400 text-sm font-medium mb-2">📋 アップロード手順</p>
    <ol className="text-blue-300 text-sm space-y-2 list-decimal list-inside">
      <li><a href="https://steamcommunity.com/sharedfiles/edititem/767/3/" className="text-blue-200 underline hover:text-blue-100">Steam ワークショップ編集ページ</a> を開く</li>
      <li>F12 で開発者ツールを開く</li>
      <li>コンソールで以下のコードを順に実行</li>
    </ol>
  </div>

  <div className="space-y-2">
    <h4 className="text-steam-textPrimary font-medium">🎭 匿名コード（作品名を非表示）</h4>
    <CopyCodeBlock label="匿名コード" code={anonymousCode} copyLabel="コピー" copiedLabel="コピーしました" />
    <p className="text-steam-textMuted text-xs">実行後、作品タイトルが非表示になります</p>
  </div>

  <div className="space-y-2">
    <h4 className="text-steam-textPrimary font-medium">🎨 アートワークコード（サイズ設定）</h4>
    <CopyCodeBlock label="アートワークコード" code={artworkCode} copyLabel="コピー" copiedLabel="コピーしました" />
    <p className="text-steam-textMuted text-xs">幅 1000px・高さ 1px で横スクロール効果を実現</p>
  </div>

  <div className="p-4 bg-green-900/20 border border-green-500/30 rounded">
    <p className="text-green-400 text-sm font-medium mb-2">✅ 完了</p>
    <p className="text-green-300 text-sm">
      コード実行後、5 分割 GIF をアップロードすると Steam プロフィールでアートワークとして表示できます！
    </p>
  </div>
</div>
